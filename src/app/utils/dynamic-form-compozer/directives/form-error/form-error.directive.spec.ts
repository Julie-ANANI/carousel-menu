import { FormErrorDirective } from './form-error.directive';

describe('FormErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormErrorDirective(null);
    expect(directive).toBeTruthy();
  });
});
