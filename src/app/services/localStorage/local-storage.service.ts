import {UmiusLocalStorageService} from '@umius/umi-common-component';

/**
 * UmiusLocalStorageService functions:
 *
 * 1. setItem(k: string, v: string): void;
 * 2. getItem(k: string): string | null;
 */

export class LocalStorageService extends UmiusLocalStorageService {}
